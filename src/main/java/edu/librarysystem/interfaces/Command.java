package edu.librarysystem.interfaces;

/**
 * Represents a command that can be executed in the library system.
 */
public interface Command {
    /**
     * Executes the command.
     */
    void execute();
}
